---
phase: 01-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - environment.yml
  - requirements.txt
  - README.md
  - tests/__init__.py
  - tests/test_environment.py
  - tests/test_project_structure.py
  - tests/test_flavordb.py
  - tests/test_foodb.py
  - tests/test_recipes.py
  - tests/test_allrecipes.py
  - tests/test_outputs.py
autonomous: true
requirements:
  - ENV-01
  - ENV-02
  - ENV-03
  - ENV-04

must_haves:
  truths:
    - "conda activate flavor-network succeeds without solver errors"
    - "python -c 'import torch, torch_geometric, rdkit' runs without errors on Apple Silicon"
    - "torch.backends.mps.is_available() returns True on M2"
    - "All required directories (data/, graph/, model/, scoring/, app/, logs/) exist"
    - "pytest tests/test_environment.py passes after environment is set up"
    - "README.md contains a clear quickstart and AllRecipes manual fallback instructions"
  artifacts:
    - path: "environment.yml"
      provides: "Conda+pip hybrid environment spec with conda-forge RDKit first, PyTorch via pip"
      contains: "rdkit=2025.03, torch==2.6.*, torch_geometric==2.7.*"
    - path: "requirements.txt"
      provides: "Pip-only packages for reference (not for conda env creation)"
      contains: "torch, torch_geometric, requests-cache, datasets, rapidfuzz, pandas, tqdm, beautifulsoup4, pytest"
    - path: "README.md"
      provides: "Quickstart setup instructions and AllRecipes manual fallback path"
      contains: "conda env create, AllRecipes CSV format, data/raw/foodb/ manual download instructions"
    - path: "tests/test_environment.py"
      provides: "Smoke tests for ENV-01, ENV-02, ENV-03"
      exports: ["test_imports", "test_mps_available", "test_versions"]
    - path: "tests/test_project_structure.py"
      provides: "Directory existence tests for ENV-04"
      exports: ["test_directories"]
  key_links:
    - from: "environment.yml"
      to: "conda env create -f environment.yml"
      via: "conda-forge channel (RDKit) then pip section (PyTorch)"
      pattern: "pip:\\s*- torch"
    - from: "tests/test_environment.py"
      to: "import torch, torch_geometric, rdkit"
      via: "activated conda environment"
      pattern: "import rdkit"
---

<objective>
Create the conda+pip hybrid environment specification, project skeleton, README quickstart, and full test scaffold for Phase 1.

Purpose: Before any scraping or data work begins, the environment definition must exist on disk and all test files must be present (even as stubs) so every subsequent task has a `pytest` command to verify against. This is the Wave 0 foundation everything else depends on.
Output: environment.yml, requirements.txt, README.md, data/ directory skeleton, and 8 test files under tests/
</objective>

<execution_context>
@/Users/sickle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sickle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md

<interfaces>
<!-- Critical install order constraint from research -->
<!-- RDKit via conda-forge MUST come before PyTorch via pip -->
<!-- Violating this causes ARM64 glibc conflicts that fail silently -->

environment.yml structure (from RESEARCH.md confirmed pattern):
```yaml
name: flavor-network
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - rdkit=2025.03
  - numpy
  - pip
  - pip:
    - torch==2.6.*
    - torchvision
    - torchaudio
    - torch_geometric==2.7.*
    - requests
    - requests-cache==1.2.*
    - datasets==3.*
    - rapidfuzz==3.*
    - pandas==2.*
    - tqdm==4.*
    - beautifulsoup4==4.*
    - pytest==7.*
```

FlavorDB2 URL (RESEARCH.md pitfall confirmed):
- BASE_URL = "https://cosylab.iiitd.edu.in/flavordb/entities_json?id="
- NOTE: No "2" in the API path — confirmed working

MPS verification pattern:
```python
import torch
device = "mps" if torch.backends.mps.is_available() else "cpu"
```

Project structure spec (RESEARCH.md architecture):
data/raw/, data/processed/, graph/, model/embeddings/, scoring/, app/, logs/, tests/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Environment files and project skeleton</name>
  <files>environment.yml, requirements.txt, data/raw/.gitkeep, data/processed/.gitkeep, graph/.gitkeep, model/embeddings/.gitkeep, scoring/.gitkeep, app/.gitkeep, logs/.gitkeep</files>
  <action>
Create environment.yml with conda-forge channel first (RDKit=2025.03, numpy, pip) and pip section second (torch==2.6.*, torchvision, torchaudio, torch_geometric==2.7.*, requests, requests-cache==1.2.*, datasets==3.*, rapidfuzz==3.*, pandas==2.*, tqdm==4.*, beautifulsoup4==4.*, pytest==7.*). Name the env "flavor-network". Do NOT add pytorch to conda channels — only pip section.

Create requirements.txt listing the same pip packages (without conda packages) as a reference file for Phase 2+ additions.

Create the directory structure with .gitkeep files:
- data/raw/
- data/processed/
- graph/
- model/embeddings/  (nested)
- scoring/
- app/
- logs/

Note: Do NOT create tests/ here — Task 2 handles that.

Anti-pattern to avoid: Do not add `- pytorch` under conda channels/dependencies. PyTorch via conda causes ARM64 glibc conflicts with conda-forge RDKit. It must be under `pip:` only.
  </action>
  <verify>
    <automated>python -c "import yaml; d=yaml.safe_load(open('environment.yml')); assert d['name']=='flavor-network'; assert any('rdkit' in str(dep) for dep in d['dependencies']); pip_deps=next(v for dep in d['dependencies'] if isinstance(dep,dict) for k,v in dep.items() if k=='pip'); assert any('torch==' in s for s in pip_deps), 'torch not in pip section'"</automated>
  </verify>
  <done>environment.yml has flavor-network name, rdkit in conda deps, torch in pip section. All 7 directories exist with .gitkeep files.</done>
</task>

<task type="auto">
  <name>Task 2: Test scaffold and README</name>
  <files>tests/__init__.py, tests/test_environment.py, tests/test_project_structure.py, tests/test_flavordb.py, tests/test_foodb.py, tests/test_recipes.py, tests/test_allrecipes.py, tests/test_outputs.py, README.md</files>
  <action>
Create tests/__init__.py (empty file).

Create tests/test_environment.py with three tests that will PASS once environment is set up:
- test_imports: import torch, torch_geometric, rdkit — assert they are importable
- test_mps_available: assert torch.backends.mps.is_available() is True (skip with pytest.skip if not on Darwin/Apple Silicon)
- test_versions: assert torch.__version__.startswith("2.6"), assert rdkit.__version__ starts with "2025"

Create tests/test_project_structure.py:
- test_directories: assert all required dirs exist: data/raw, data/processed, graph, model/embeddings, scoring, app, logs, tests

Create tests/test_flavordb.py with stubs (skip markers):
- test_cache_populated: skip with reason "FlavorDB2 scraper not yet run"; when active — check data/raw/flavordb_cache.sqlite exists AND pd.read_csv("data/raw/ingredients.csv") has >= 900 rows
- test_ingredients_schema: skip; when active — check ingredients.csv has columns: ingredient_id, name, category, molecules_json

Create tests/test_foodb.py:
- test_join_count: skip with reason "FooDB join not yet run"; when active — check molecules.csv has >= 300 rows AND has a foodb_matched column

Create tests/test_recipes.py:
- test_cooccurrence_count: skip with reason "RecipeNLG not yet processed"; when active — check recipes.csv has > 1000 rows with columns: ingredient_a, ingredient_b, count

Create tests/test_allrecipes.py:
- test_scraper_runs: skip with reason "AllRecipes scraper not yet run"; when active — check that either data/raw/recipes_allrecipes.csv exists OR logs/pipeline.log contains "AllRecipes: partial" or "AllRecipes: blocked"

Create tests/test_outputs.py:
- test_output_files_exist: assert data/raw/ingredients.csv AND data/raw/molecules.csv AND data/raw/recipes.csv all exist (this test will FAIL until data ingestion completes — that is expected and correct)

Create README.md with:
1. Project description (1 paragraph)
2. Quickstart section:
   - Step 1: conda env create -f environment.yml
   - Step 2: conda activate flavor-network
   - Step 3: python run_pipeline.py (with note about --skip-* flags)
3. Manual data setup section:
   - FooDB: Download tar.gz from foodb.ca/downloads (CC BY-NC 4.0), extract to data/raw/foodb/
   - AllRecipes fallback: If scraper is blocked, create data/raw/recipes_allrecipes.csv with columns: recipe_name, ingredients (comma-separated list)
4. Pipeline flags: --skip-scrape, --skip-foodb, --skip-recipes, --force
5. Output files: data/raw/ingredients.csv, molecules.csv, recipes.csv

Use pytest.mark.skip (not xfail) for the data-dependent tests. The stubs should be real test functions with docstrings explaining what they will verify once data exists.
  </action>
  <verify>
    <automated>cd /Users/sickle/Coding/Flavor-Network-Analysis && python -m pytest tests/test_project_structure.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>tests/test_project_structure.py::test_directories passes. All 8 test files exist. README.md has Quickstart, FooDB manual instructions, AllRecipes fallback format, and pipeline flags documented.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `python -c "import yaml; yaml.safe_load(open('environment.yml'))"` — valid YAML, no error
2. `pytest tests/test_project_structure.py -x -q` — passes (green)
3. `pytest tests/ -q` — test_project_structure passes; all other tests either pass or show as skipped (none should ERROR)
4. `ls data/ graph/ model/ scoring/ app/ logs/` — all directories exist
5. `cat README.md` — contains "conda env create", "foodb.ca/downloads", "recipes_allrecipes.csv"
</verification>

<success_criteria>
- environment.yml exists with rdkit in conda deps and torch under pip section
- All 7 project directories exist
- 8 test files exist under tests/; test_project_structure passes; others skip or pass
- README.md contains complete quickstart and both manual fallback paths (FooDB and AllRecipes)
- No test ERRORs (only PASS or SKIP)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>

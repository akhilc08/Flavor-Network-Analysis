---
phase: 06-streamlit-ui
plan: "06"
type: execute
wave: 4
depends_on:
  - "06-04"
  - "06-05"
files_modified: []
autonomous: false
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
  - UI-07

must_haves:
  truths:
    - "All 4 pages load in browser without Python tracebacks"
    - "Page 1 returns search results with radar charts for a real ingredient query"
    - "Page 3 renders PyVis graph with selectbox pivot working correctly"
    - "Page 4 streams a recipe that names specific shared compounds"
    - "All automated tests pass before human verification begins"
  artifacts:
    - path: "app/app.py"
      provides: "Working Streamlit entry point"
    - path: "app/pages/1_Search.py"
      provides: "Search page with radar charts and molecule tags"
    - path: "app/pages/2_Rate.py"
      provides: "Rating page with AUC delta display"
    - path: "app/pages/3_Graph.py"
      provides: "Graph explorer with PyVis + selectbox pivot"
    - path: "app/pages/4_Recipe.py"
      provides: "Streaming recipe page with Claude integration"
  key_links:
    - from: "streamlit run app/app.py"
      to: "all 4 pages"
      via: "Streamlit auto-discovers pages/ directory"
      pattern: "app/pages/"
---

<objective>
Run the full automated test suite, then verify the complete Streamlit app in the browser across all 4 pages.

Purpose: Phase 6 acceptance gate — confirms every UI requirement is met before the phase is marked complete.
Output: Human-verified working demo app.
</objective>

<execution_context>
@/Users/sickle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sickle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-streamlit-ui/06-CONTEXT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated test suite</name>
  <files></files>
  <action>
    Run the complete test suite and confirm all tests pass:

    ```bash
    pytest tests/ -v 2>&1 | tee /tmp/phase6_test_results.txt
    cat /tmp/phase6_test_results.txt | tail -20
    ```

    Expected outcome:
    - tests/test_ui_search.py: 3 tests PASS
    - tests/test_ui_rate.py: 2 tests PASS
    - tests/test_ui_graph.py: 3 tests PASS
    - tests/test_ui_cache.py: 2 tests PASS
    - tests/test_ui_errors.py: 2 tests PASS
    - All prior phase tests: PASS or SKIP (none newly failing)

    If any test fails, fix before proceeding to the checkpoint.

    Also verify all page files exist and have valid syntax:
    ```bash
    python -c "
    import ast, pathlib
    pages = ['app/app.py', 'app/pages/1_Search.py', 'app/pages/2_Rate.py',
             'app/pages/3_Graph.py', 'app/pages/4_Recipe.py']
    for p in pages:
        src = pathlib.Path(p).read_text()
        ast.parse(src)
        print(f'{p}: OK')
    "
    ```
  </action>
  <verify>
    <automated>pytest tests/test_ui_search.py tests/test_ui_rate.py tests/test_ui_graph.py tests/test_ui_cache.py tests/test_ui_errors.py -v --tb=short 2>&1 | tail -15</automated>
  </verify>
  <done>All 12 automated UI tests pass. All 5 page files have valid Python syntax. No test regressions in prior phases.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete 4-page Streamlit demo app:
    - Page 1 (Search): ingredient search with 10 expandable cards, radar charts, molecule tags, "Why it works"
    - Page 2 (Rate): 5 uncertain pairs with star sliders, AUC delta after fine-tuning
    - Page 3 (Graph): PyVis network explorer with selectbox pivot, edge color coding
    - Page 4 (Recipe): Streaming Claude recipe generation with molecular rationale
    - All pages: earthy food-forward theme, no raw tracebacks
  </what-built>
  <how-to-verify>
    IMPORTANT: Phases 2-5 must be complete for full functionality. If not yet complete, verify the UI structure using mock/stub data where possible, and note which pages are blocked on upstream data.

    1. Start the app:
       ```bash
       cd /Users/sickle/Coding/Flavor-Network-Analysis
       streamlit run app/app.py
       ```
       Expected: App opens at http://localhost:8501. Landing page shows navigation sidebar.

    2. Verify the earthy theme:
       - Background is warm cream (#fdf6ec), not white
       - Sidebar is tan (#f0e6d3)
       - Headings use Georgia serif font
       - No default Streamlit blue theme visible

    3. Page 1 — Ingredient Search:
       - Navigate to "Search" in sidebar
       - Type an ingredient name (e.g. "strawberry" or "garlic")
       - Verify: 10 expandable cards appear; first is expanded by default
       - Each card shows: colored pill badge (Surprising/Unexpected/Classic), score bar, molecule tags, "Why it works" text
       - At least one card shows a Plotly radar chart with two overlapping polygons
       - Searching a nonexistent ingredient shows a friendly info message (not a traceback)

    4. Page 2 — Rate Uncertain Pairs:
       - Navigate to "Rate" in sidebar
       - If AUC < 0.70: verify warning message appears and Submit button is disabled
       - If AUC >= 0.70: verify 5 pairs appear with sliders; try moving a slider; click Submit
       - After submit: verify spinner appears, then AUC before/after shown as st.metric

    5. Page 3 — Flavor Graph Explorer:
       - Navigate to "Graph" in sidebar
       - Select an ingredient from the dropdown
       - Verify: PyVis network graph renders with colored nodes and edges
       - Select a different ingredient from the dropdown — verify graph re-renders centered on new selection
       - Verify: no more than 50 nodes visible
       - Verify: edge colors differ (some red, some blue)

    6. Page 4 — Recipe Generation:
       - Navigate to "Recipe" in sidebar
       - If ANTHROPIC_API_KEY not set: verify friendly error message (not KeyError)
       - If ANTHROPIC_API_KEY is set:
         a. Select 2-3 ingredients from the multiselect
         b. Click "Generate Recipe"
         c. Verify recipe streams token-by-token (not all at once)
         d. Verify recipe names specific shared flavor compounds
         e. Verify recipe includes dish name, ingredients list, and cooking steps

    7. Error handling:
       - Temporarily rename scoring/scored_pairs.pkl (if it exists)
       - Visit each page — verify each shows a warning about running the pipeline, not a traceback
       - Restore the file

    Report any issues found.
  </how-to-verify>
  <resume-signal>Type "approved" if all pages work correctly, or describe specific issues for gap closure.</resume-signal>
</task>

</tasks>

<verification>
Phase 6 complete when:
- All 12 automated UI tests pass (pytest tests/test_ui_*.py)
- Human verification approved for all 4 pages
- No raw Python tracebacks visible under any user-facing error path
- ROADMAP.md Phase 6 marked complete
</verification>

<success_criteria>
- Page 1: top 10 pairings with radar chart and molecule tags for any searched ingredient
- Page 2: 5 uncertain pairs, star ratings, AUC delta after fine-tuning, gate check working
- Page 3: PyVis graph <= 50 nodes, edge colors, selectbox pivot
- Page 4: streaming recipe with molecular rationale via claude-sonnet-4-6
- All pages: earthy theme, no raw tracebacks, friendly error messages
</success_criteria>

<output>
After completion, create `.planning/phases/06-streamlit-ui/06-06-SUMMARY.md`
</output>

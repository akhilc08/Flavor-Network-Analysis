---
phase: 04-model-training
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_model.py
  - tests/conftest.py
autonomous: true
requirements:
  - MODEL-01
  - MODEL-02
  - MODEL-03
  - MODEL-04
  - MODEL-05
  - MODEL-06
  - MODEL-07
  - MODEL-08
  - MODEL-09

must_haves:
  truths:
    - "pytest tests/test_model.py -x -q exits 0 (all stubs pass or xfail)"
    - "A synthetic HeteroData fixture is importable from conftest.py with 10 ingredient nodes, 50 molecule nodes, and all 3 edge types"
    - "Every MODEL-01 through MODEL-09 requirement has at least one named test function in test_model.py"
  artifacts:
    - path: "tests/test_model.py"
      provides: "Stub tests for all 9 MODEL requirements"
      exports: ["test_gat_output_shape", "test_no_self_loops", "test_bn_dropout_present", "test_molecular_loss", "test_recipe_loss", "test_infonce_loss", "test_combined_loss_formula", "test_checkpoint_save_on_improvement", "test_embedding_export"]
    - path: "tests/conftest.py"
      provides: "Shared synthetic HeteroData fixture"
      contains: "tiny_hetero_graph"
  key_links:
    - from: "tests/test_model.py"
      to: "tests/conftest.py"
      via: "pytest fixture injection"
      pattern: "tiny_hetero_graph"
---

<objective>
Create the Wave 0 test scaffold for Phase 4: a conftest.py fixture providing a tiny synthetic HeteroData graph and a test_model.py file with stub/failing tests for all 9 MODEL requirements. This gates all subsequent plans — no implementation begins until the test scaffold exists.

Purpose: Nyquist compliance — every task in plans 02-04 has an automated verify command that references tests/test_model.py. This plan creates that file so executors do not hit a missing-file error.
Output: tests/test_model.py with 9+ named test functions (stubs), tests/conftest.py with a tiny_hetero_graph fixture. All stubs must pass (as pytest.mark.xfail or skip with a clear TODO comment) or they must fail with ImportError only (not syntax errors).
</objective>

<execution_context>
@/Users/sickle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sickle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-model-training/04-RESEARCH.md
@.planning/phases/04-model-training/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create conftest.py with synthetic HeteroData fixture</name>
  <files>tests/conftest.py</files>
  <action>
Add a pytest fixture `tiny_hetero_graph` to tests/conftest.py (append to existing file if it exists, otherwise create it).

The fixture must return a `torch_geometric.data.HeteroData` object with:
- `data['ingredient'].x`: float32 tensor of shape (10, 20) — 10 ingredient nodes, 20 features each (random, deterministic via `torch.manual_seed(42)`)
- `data['molecule'].x`: float32 tensor of shape (50, 15) — 50 molecule nodes, 15 features each
- `data['ingredient', 'contains', 'molecule'].edge_index`: shape (2, 30) — 30 directed edges, ingredient→molecule
- `data['molecule', 'rev_contains', 'ingredient'].edge_index`: shape (2, 30) — reverse of contains
- `data['ingredient', 'co_occurs', 'ingredient'].edge_index`: shape (2, 20) — ingredient→ingredient co-occurrence edges (no self-loops: ensure src != dst)
- `data['molecule', 'structurally_similar', 'molecule'].edge_index`: shape (2, 40) — molecule→molecule similarity edges (no self-loops)

Also add a `tiny_link_labels` fixture that returns a dict with:
- `pos_edge_index`: (2, 5) tensor of ingredient→ingredient positive validation edges (indices within [0, 9])
- `edge_label`: (10,) tensor — 5 ones followed by 5 zeros (positive then negative)
- `neg_edge_index`: (2, 5) tensor of negative edges

Use `scope='module'` on the fixtures to avoid re-creation per test.

Check if conftest.py already exists (`tests/conftest.py`). If it does, read it first and append the new fixtures below any existing content — do NOT overwrite existing fixtures from Phase 1.
  </action>
  <verify>
    <automated>cd /Users/sickle/Coding/Flavor-Network-Analysis && python -c "import pytest; import tests.conftest" 2>&1 | head -5; python -c "from torch_geometric.data import HeteroData; print('HeteroData ok')"</automated>
  </verify>
  <done>conftest.py importable with no syntax errors; tiny_hetero_graph fixture defined and returns a HeteroData with all 4 edge types.</done>
</task>

<task type="auto">
  <name>Task 2: Create test_model.py with stubs for MODEL-01 through MODEL-09</name>
  <files>tests/test_model.py</files>
  <action>
Create `tests/test_model.py` from scratch. The file must contain exactly these named test functions (all marked `pytest.mark.xfail(reason="not implemented yet", strict=False)` so the suite is green before implementation):

```
test_gat_output_shape          # MODEL-01: FlavorGAT forward produces (num_ingredients, 128) output
test_no_self_loops             # MODEL-02: GATConv layers have add_self_loops=False
test_bn_dropout_present        # MODEL-03: BatchNorm1d and dropout present in model
test_molecular_loss            # MODEL-04: molecular BCE loss returns scalar tensor
test_recipe_loss               # MODEL-05: recipe BCE loss returns scalar tensor
test_infonce_loss              # MODEL-06: InfoNCE returns scalar, not nan, logged separately
test_combined_loss_formula     # MODEL-07: combined = alpha*mol + beta*rec + gamma*nce within 1e-5
test_checkpoint_save_on_improvement  # MODEL-08: checkpoint saved when AUC improves
test_embedding_export          # MODEL-09: pkl contains {ingredient_id: np.ndarray of shape (128,)}
```

Each test body should import the relevant module and assert the behavior described. Since the implementation files do not yet exist, each test should catch `ImportError` gracefully with a clear failure message (do NOT use try/except to silently pass — let the xfail handle it).

Example stub pattern:
```python
@pytest.mark.xfail(reason="model/gat_model.py not yet implemented", strict=False)
def test_gat_output_shape(tiny_hetero_graph):
    from model.gat_model import FlavorGAT
    model = FlavorGAT(hidden_channels=64, embed_dim=128, heads=4, dropout=0.0)
    out = model(tiny_hetero_graph.x_dict, tiny_hetero_graph.edge_index_dict)
    assert out['ingredient'].shape == (10, 128), f"Expected (10, 128), got {out['ingredient'].shape}"
```

For `test_checkpoint_save_on_improvement`, use `tmp_path` fixture from pytest — no external filesystem writes.

For `test_embedding_export`, check that the pkl dict has string keys and numpy array values of shape `(128,)`.

Add `import pytest, torch, numpy as np` at the top. Import `Path` from pathlib. Do not import model modules at the top level — always import inside the test function body so ImportError is caught by xfail.
  </action>
  <verify>
    <automated>cd /Users/sickle/Coding/Flavor-Network-Analysis && python -m pytest tests/test_model.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>pytest tests/test_model.py -x -q exits 0 with all 9 tests xfailed or xpassed (no ERROR, no FAILED). Suite is green.</done>
</task>

</tasks>

<verification>
Run full Phase 1 suite to confirm no existing tests were broken:

```bash
cd /Users/sickle/Coding/Flavor-Network-Analysis && python -m pytest tests/ -x -q 2>&1 | tail -15
```

Confirm test_model.py has exactly 9 test functions with the required names:
```bash
cd /Users/sickle/Coding/Flavor-Network-Analysis && python -m pytest tests/test_model.py --collect-only -q 2>&1
```
</verification>

<success_criteria>
- tests/conftest.py contains tiny_hetero_graph and tiny_link_labels fixtures (scope='module')
- tests/test_model.py contains 9 named test functions (test_gat_output_shape through test_embedding_export)
- `pytest tests/test_model.py -x -q` exits 0 (all xfail, no ERROR)
- `pytest tests/ -x -q` exits 0 (existing Phase 1 tests unaffected)
</success_criteria>

<output>
After completion, create `.planning/phases/04-model-training/04-01-SUMMARY.md`
</output>

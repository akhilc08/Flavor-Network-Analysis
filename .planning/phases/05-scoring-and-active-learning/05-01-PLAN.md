---
phase: 05-scoring-and-active-learning
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_scoring.py
  - tests/test_active_learning.py
  - scoring/__init__.py
  - scoring/score.py
  - model/active_learning.py
autonomous: true
requirements:
  - SCORE-01
  - SCORE-02
  - SCORE-03
  - SCORE-04
  - LEARN-01
  - LEARN-02
  - LEARN-03
  - LEARN-04
  - LEARN-05
  - LEARN-06

must_haves:
  truths:
    - "pytest tests/test_scoring.py tests/test_active_learning.py -x -q runs without import errors (tests may xfail/skip but must be importable)"
    - "scoring/ is importable as a Python package (scoring/__init__.py exists)"
    - "scoring/score.py exports the four required public functions with correct signatures"
    - "model/active_learning.py exports submit_rating() and is_active_learning_enabled() with correct signatures"
    - "graph/val_edges.pt exists (Phase 4 boundary check passes)"
    - "model/training_metadata.json exists with best_val_auc key (Phase 4 boundary check passes)"
  artifacts:
    - path: "tests/test_scoring.py"
      provides: "Failing test stubs for SCORE-01, SCORE-02, SCORE-03, SCORE-04, LEARN-02"
      exports: ["test_surprise_formula", "test_score_components", "test_scored_pairs_file", "test_labels", "test_uncertain_pairs"]
    - path: "tests/test_active_learning.py"
      provides: "Failing test stubs for LEARN-01, LEARN-03, LEARN-04, LEARN-05, LEARN-06"
      exports: ["test_feedback_csv", "test_finetune_loop", "test_checkpoint_and_rescore", "test_auc_gate", "test_submit_rating_return"]
    - path: "scoring/__init__.py"
      provides: "Package init enabling 'from scoring.score import ...' imports"
    - path: "scoring/score.py"
      provides: "API stubs: compute_all_pairs, load_scored_pairs, get_top_pairings, get_uncertain_pairs"
    - path: "model/active_learning.py"
      provides: "API stubs: submit_rating, is_active_learning_enabled"
  key_links:
    - from: "tests/test_scoring.py"
      to: "scoring/score.py"
      via: "from scoring.score import compute_all_pairs, load_scored_pairs, get_top_pairings, get_uncertain_pairs"
      pattern: "from scoring\\.score import"
    - from: "tests/test_active_learning.py"
      to: "model/active_learning.py"
      via: "from model.active_learning import submit_rating, is_active_learning_enabled"
      pattern: "from model\\.active_learning import"
---

<objective>
Create the test scaffold and API stubs that gate all subsequent waves.
This is a Wave 0 plan: it writes failing tests and stub implementations so that
every later task has a runnable verify command on first execution.

Purpose: Nyquist compliance — no task in Waves 1–2 is allowed to proceed without an automated
verify command. This plan creates those commands. It also asserts Phase 4 boundary artifacts
exist so Wave 1 and Wave 2 can assume they are present without re-checking.

Output:
- tests/test_scoring.py (5 test functions, initially failing)
- tests/test_active_learning.py (5 test functions, initially failing)
- scoring/__init__.py (empty package init)
- scoring/score.py (API stubs that raise NotImplementedError)
- model/active_learning.py (API stubs that raise NotImplementedError)
</objective>

<execution_context>
@/Users/sickle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sickle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-scoring-and-active-learning/05-CONTEXT.md
@.planning/phases/05-scoring-and-active-learning/05-RESEARCH.md
@.planning/phases/05-scoring-and-active-learning/05-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write test stubs for scoring module (SCORE-01..04, LEARN-02)</name>
  <files>tests/test_scoring.py</files>
  <behavior>
    - test_surprise_formula: Given a mock DataFrame with 3 rows of known pairing_score, recipe_familiarity, molecular_overlap values, assert surprise_score matches formula ps × (1 - rf) × (1 - mo × 0.5) within 1e-6 tolerance. Test must FAIL (NotImplementedError) until Wave 1 implements compute_all_pairs.
    - test_score_components: Given 3 synthetic ingredients (random 128-dim embeddings as np.ndarray, molecule sets as frozensets of ints, co_occurrence dict), call compute_all_pairs() and assert: (a) pairing_score column values are all in [0, 1]; (b) molecular_overlap column values are all in [0, 1]; (c) recipe_familiarity column values are all in [0, 1]. Test must FAIL until Wave 1.
    - test_scored_pairs_file: marked with @pytest.mark.skipif(not Path("model/embeddings/ingredient_embeddings.pkl").exists(), reason="Requires Phase 4 outputs"). When artifact exists: assert Path("scoring/scored_pairs.pkl").exists() and df loaded is sorted by surprise_score descending. Stub: SKIP until integration run.
    - test_labels: Given a DataFrame with known surprise_score values spanning [0, 1], assert label column contains only values in {"Surprising", "Unexpected", "Classic"} and has no NaN. Test must FAIL until Wave 1.
    - test_uncertain_pairs: Call get_uncertain_pairs(n=5) on a fabricated scored_pairs DataFrame (monkey-patched via monkeypatch of load_scored_pairs), assert result is a list of 5 dicts and abs(item["pairing_score"] - 0.5) is non-decreasing. Test must FAIL until Wave 1.
  </behavior>
  <action>
    Create tests/test_scoring.py. All imports at top: from pathlib import Path, import pytest,
    from unittest.mock import patch, import pandas as pd, import numpy as np,
    from scoring.score import compute_all_pairs, load_scored_pairs, get_top_pairings, get_uncertain_pairs.

    Use synthetic fixtures only — no file I/O except test_scored_pairs_file which is guarded by skipif.

    For test_score_components fixture: use 4 ingredients with embeddings = np.random.randn(128).astype(np.float32),
    molecule sets = {frozenset({1,2,3}), frozenset({2,4}), frozenset({5}), frozenset()},
    co_occurrence = {("a","b"): 10, ("b","c"): 3, ("a","d"): 0}.

    For test_uncertain_pairs: build a DataFrame with 20 rows where pairing_score is
    np.linspace(0.1, 0.9, 20), use monkeypatch.setattr to replace load_scored_pairs with
    a lambda returning this DataFrame.

    Do NOT add pytest.ini markers — the test runner already discovers tests/ by convention.
    Run pattern: pytest tests/test_scoring.py -x -q
  </action>
  <verify>
    <automated>cd /Users/sickle/Coding/Flavor-Network-Analysis && python -m pytest tests/test_scoring.py -x -q 2>&1 | head -30</automated>
  </verify>
  <done>pytest tests/test_scoring.py collects 5 tests and runs without ImportError. Some/all tests FAIL with NotImplementedError or similar (stubs not implemented yet) — that is expected and correct at Wave 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write test stubs for active learning module (LEARN-01, LEARN-03..06) and create API stubs</name>
  <files>
    tests/test_active_learning.py
    scoring/__init__.py
    scoring/score.py
    model/active_learning.py
  </files>
  <behavior>
    tests/test_active_learning.py:
    - test_feedback_csv: Call append_feedback (import from model.active_learning) with ("apple", "chocolate", 4), assert feedback.csv exists and the last row has those values. Uses tmp_path fixture to avoid polluting project root. Test must FAIL until Wave 2.
    - test_finetune_loop: Using a minimal 1-layer GATConv synthetic model (not best_model.pt), call fine_tune_with_replay() with 2 feedback pairs and a tiny replay buffer dict. Assert it runs 10 epochs (by checking a call count or that it does not raise) and returns a dict. Test must FAIL until Wave 2.
    - test_checkpoint_and_rescore: marked @pytest.mark.skipif(not Path("model/embeddings/ingredient_embeddings.pkl").exists()). When artifacts present: call submit_rating("apple","chocolate",4), assert Path("model/checkpoints/pre_finetune_round_1.pt").exists(). Stub: SKIP until integration run.
    - test_auc_gate: (a) Assert is_active_learning_enabled() returns False when model/training_metadata.json does not exist (tmp_path no-file case). (b) Assert is_active_learning_enabled() returns False when best_val_auc=0.65. (c) Assert is_active_learning_enabled() returns True when best_val_auc=0.75. Use monkeypatch of METADATA_PATH constant. Test must FAIL until Wave 2.
    - test_submit_rating_return: marked @pytest.mark.skipif(not Path("model/embeddings/ingredient_embeddings.pkl").exists()). When artifacts present: assert submit_rating("apple","chocolate",4) returns a dict with keys "auc_before" and "auc_after" both floats. Stub: SKIP until integration run.

    scoring/__init__.py: Empty file (package marker only).

    scoring/score.py: Stub with all 4 public function signatures and NotImplementedError bodies.
    Docstrings must match the locked API exactly:
      compute_all_pairs(embeddings: dict, co_occurrence: dict, molecule_sets: dict) -> pd.DataFrame
      load_scored_pairs() -> pd.DataFrame
      get_top_pairings(ingredient_name: str, n: int = 10) -> list[dict]
      get_uncertain_pairs(n: int = 20) -> list[dict]

    model/active_learning.py: Stub with 2 public function signatures + NotImplementedError bodies.
    Also stub the internal helpers used in tests: append_feedback, fine_tune_with_replay.
    Public API:
      submit_rating(ingredient_a: str, ingredient_b: str, rating: int) -> dict
      is_active_learning_enabled() -> bool
    Internal helpers (also exported for unit testing):
      append_feedback(ingredient_a: str, ingredient_b: str, rating: int) -> None
      fine_tune_with_replay(model, hetero_data, feedback_pairs, replay_buffer, val_edges, optimizer, n_epochs=10) -> dict

    Also declare METADATA_PATH = Path("model/training_metadata.json") and AUC_GATE = 0.70
    as module-level constants (needed for monkeypatching in test_auc_gate).

    Phase 4 boundary assertions (run as module-level code guarded by a check function,
    NOT as bare assertions that crash on import):
    Define check_phase4_artifacts() -> None that logs a WARNING (not raises) if
    graph/val_edges.pt or model/training_metadata.json is missing.
    Call check_phase4_artifacts() inside submit_rating() before fine-tuning begins.
  </action>
  <verify>
    <automated>cd /Users/sickle/Coding/Flavor-Network-Analysis && python -m pytest tests/test_scoring.py tests/test_active_learning.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>pytest collects 10 tests across both files. All unit tests (not marked skipif) run and FAIL with NotImplementedError. The 3 integration tests marked skipif are SKIPPED (Phase 4 artifacts not present yet). No ImportError from any test file. scoring/ and model/active_learning.py are importable.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Import check: python -c "from scoring.score import compute_all_pairs, load_scored_pairs, get_top_pairings, get_uncertain_pairs; print('scoring OK')"
2. Import check: python -c "from model.active_learning import submit_rating, is_active_learning_enabled, METADATA_PATH, AUC_GATE; print('active_learning OK')"
3. Full test collect: pytest tests/test_scoring.py tests/test_active_learning.py --collect-only -q (must show 10 test items, no collection errors)
4. Test run: pytest tests/test_scoring.py tests/test_active_learning.py -x -q (unit tests FAIL on NotImplementedError; integration tests SKIP — both outcomes are correct at Wave 0)
</verification>

<success_criteria>
- 10 tests collected, 0 collection errors
- 7 unit tests FAIL with NotImplementedError (not ImportError, not AttributeError)
- 3 integration tests SKIP (skipif guard triggers because Phase 4 artifacts do not exist yet)
- scoring/__init__.py, scoring/score.py, model/active_learning.py all importable
- model/active_learning.py declares METADATA_PATH, AUC_GATE, append_feedback, fine_tune_with_replay
</success_criteria>

<output>
After completion, create `.planning/phases/05-scoring-and-active-learning/05-01-SUMMARY.md`
</output>

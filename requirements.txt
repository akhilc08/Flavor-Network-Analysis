# Pip-only packages for reference (not for conda env creation)
# Use environment.yml for the full conda+pip hybrid environment setup
# conda env create -f environment.yml

torch==2.6.*
torchvision
torchaudio
torch_geometric==2.7.*
requests
requests-cache==1.2.*
datasets==3.*
rapidfuzz==3.*
pandas==2.*
tqdm==4.*
beautifulsoup4==4.*
pytest==7.*
